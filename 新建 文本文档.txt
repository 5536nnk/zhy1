<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🐟 永远爱你 🌸</title>
    <style>
        /* 基础样式与响应式 */
        :root {
            --primary: #f8d7f3;  /* 粉紫色 */
            --accent: #ff6b6b;  /* 爱心红 */
        }

        body {
            margin: 0;
            background: linear-gradient(135deg, var(--primary) 0%, #d1b3ff 100%);
            font-family: 'HarmonyOS Sans', sans-serif;
            min-height: 100vh;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* 动态背景 */
        .water-effect {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }

        /* 表情雨动画 */
        .emoji-rain {
            position: fixed;
            animation: fall 3s linear infinite;
            opacity: 0.7;
            cursor: pointer;
            user-select: none;
        }

        @keyframes fall {
            0% { transform: translateY(-50px) rotate(0deg); }
            100% { transform: translateY(100vh) rotate(360deg); }
        }

        /* 首页布局 */
        .hero-section {
            text-align: center;
            padding: 100px 20px;
            color: #4a2c40;
        }

        .couple-avatar {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            border: 3px solid white;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            margin: 20px auto;
        }

        /* 移动端适配 */
        @media (max-width: 768px) {
            .couple-avatar { width: 150px; height: 150px; }
            .hero-section { padding: 60px 10px; }
            .emoji-rain { font-size: 1.2em; }
        }
    </style>
</head>
<body>
    <!-- 动态水波纹 -->
    <div class="water-effect"></div>

    <!-- 主内容 -->
    <div class="container">
        <section class="hero-section">
            <h1>🐟 张昊宇 ❤️ 苟宇杰大帅哥 🐶</h1>
            <img src="your-photo.jpg" class="couple-avatar" alt="少焦虑多睡觉，不要c我">
            <p>昊宇宝宝永远开心</p>
            <div class="timeline"></div>
        </section>
    </div>

    <script>
        // 表情雨生成器
        function createEmojiRain() {
            const emojis = ['🐶', '❤️'];
            const container = document.body;
            
            setInterval(() => {
                const emoji = document.createElement('div');
                emoji.className = 'emoji-rain';
                emoji.textContent = emojis[Math.floor(Math.random() * emojis.length)];
                
                // 随机位置与动画
                emoji.style.left = Math.random() * 100 + 'vw';
                emoji.style.animationDelay = Math.random() * 2 + 's';
                emoji.style.fontSize = (18 + Math.random() * 10) + 'px';
                
                // 点击特效
                emoji.onclick = () => {
                    emoji.style.animation = 'none';
                    emoji.style.transition = 'transform 0.5s';
                    emoji.style.transform = 'scale(3) translateY(-50px)';
                    setTimeout(() => emoji.remove(), 500);
                };

                container.appendChild(emoji);
                setTimeout(() => emoji.remove(), 3000);
            }, 1000); // 控制密度
        }

        // 移动端检测
        function isMobile() {
            return /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
        }

        // 初始化
        window.onload = () => {
            createEmojiRain();
            if(isMobile()) document.body.classList.add('mobile');
        }
    </script>
</body>
</html>